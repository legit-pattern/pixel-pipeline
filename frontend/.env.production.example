# Backend public URL (tunnel/reverse proxy), no trailing slash.
VITE_API_BASE_URL=https://your-backend-tunnel-domain.example

# For GitHub Pages project sites, use /<repo-name>/
# For custom domain root, use /
VITE_PUBLIC_BASE=/pixel-pipeline/
